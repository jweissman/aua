# Flavfile 0.1

# Could Wrap a CLI around existing scripts:
# test:
#   generates:
#     creatures:
#       - task: assembly
#       - task: distribution
#       - task: threat
#       - task: alignment
#     house:
#       - task: consistency
#     squad:
#       - task: integrity
#     combat:
#       - task: sanity

# app:
#   generates:
# Core duel pipeline
app:
  generates:
    - task: body
    - task: identity
    - task: moiety
    - task: faction
    - task: archetype
    # script: load_yaml('design/archetypes.yml').archetypes.keys.sample
    # (which has subtypes), microtype (subtype + subtype...)
    # - task: moiety (which 'are' subtypes, but in the sim layer)

    - task: creature
      wants: body, identity, moiety
    - task: rare.creature
      wants: creature, archetype
    - task: duel
      wants: faction, creature:a, creature:b
